<%- include('../partials/header') %>

<main class="viewBody">
    <section class="userInfo">
        <img src="<%= user.avatar %>" alt="Profile Picture" class="circle responsive-img">
        <h1><%= user.name%></h1>

        <%if (user.contests) {user.contests.forEach(contest => {%>

        <%})};%>

    </section>

    <%if (user.authorizationKey) { %>
    <%- include('./partials/admin') %>
    <%}else{%>
    <%- include('./partials/player') %>
    <%}%>
<%- include('../partials/footer') %>